<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_width="fill_parent"
	android:layout_height="fill_parent"
	android:orientation="vertical"
	android:background="#fff">

	<ImageView android:src="@drawable/logo" android:layout_height="wrap_content" android:layout_width="fill_parent" android:id="@+id/img" />

	<TextView android:text="@string/list_apps"
		android:layout_width="fill_parent"
		android:layout_height="wrap_content"
		android:gravity="center_horizontal"
		android:textColor="#000"
		android:id="@+id/cab"
		android:layout_below="@id/img" />

	<ListView android:id="@+id/result_list"
		android:layout_width="fill_parent"
		android:layout_height="fill_parent"
		android:layout_below="@id/cab"
		android:textColor="#000"
		android:background="#fff"
		android:scrollbarAlwaysDrawVerticalTrack="true" >
	<!-- Aqui se insertan los resultados  -->
	</ListView>
</RelativeLayout>